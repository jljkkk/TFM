There are two main code files that were not uploaded because they exceeded the size limit (over 100 MB).

* **Note:** The two largest code files exceed GitHub's size limit and are provided as a compressed `compressed code.zip` file.
